<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN">
<html>
<head>
  <script language="javascript">
<!--
    var haveSearchHereWindow = false;
    var haveComparisonWindow = false;
    
    function openSearchHereWindow(is_popup)
    {
        // already opened?
        if (haveSearchHereWindow)
        {
            return;
        }
        haveSearchHereWindow = true;
        
        // open it     
        var str = "toolbar=yes,menubar=yes,status=yes,location=yes,left=0,screenX=0,top=0,screenY=0,resizable";
        if (window.screen) {
            var ah = screen.availHeight - 30;
            var aw = screen.availWidth - 10;
            str += ",height=" + ah;
            str += ",innerHeight=" + ah;
            str += ",width=" + aw;
            str += ",innerWidth=" + aw;
        }

        var x = window.open('http://www.searchhere.com','pop_bd_searchhere',str);
        
        if (is_popup)
        {
            // popover
            x.focus();      
        }
        else
        {
            // popunder
            x.blur();
//            window.focus();
//            window.focus();
//            window.focus();
        }
    }    

    function openComparisonWindow(is_popup)
    {
        // already opened?
        if (haveComparisonWindow)
        {
            return;
        }
        haveComparisonWindow = true;
        
        // open it     
        popW = 320;
        popH = 520;

        xcoord = screen.availWidth - (popW + 20);
        ycoord = screen.availHeight - (popH + 50);

        str = "width=" + popW + ",height=" + popH + ",top=" + ycoord + ",screenY=" + ycoord + ",left=" + xcoord + ",screenX=" + xcoord;

        popW = 525;
        popH = 550;

        str = "width=" + popW + ",height=" + popH + ",scrollbars,resizable,top=0,screenY=0,left=0,screenX=0";

        var x = window.open('http://www.buydomains.com/comparison.pop.jsp','pop_bd_comparision',str);

        if (is_popup)
        {
            // popover
            x.focus();      
        }
        else
        {
            // popunder
            x.blur();
//            window.focus();
//            window.focus();
//            window.focus();
        }
    }
    
    function onCloseHandler()
    {
        openSearchHereWindow(true);
        openComparisonWindow(true);
    }
   
    window.setTimeout('openSearchHereWindow(false)', 20000);
    window.setTimeout('openComparisonWindow(false)', 40000);     
    window.setTimeout('window.close()', 45000);
-->
  </script>
</head>
<body onunload="onCloseHandler()">
<br>
</body>
</html>

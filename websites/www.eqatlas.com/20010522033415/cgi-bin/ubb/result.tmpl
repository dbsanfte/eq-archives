<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.0 Transitional//EN"> 
 <!-- <#achk_ver#> -->
 <html>
 <head>
 <script>
 	<!--
 	function doload()
 	{
 		if (parent.bLayter)
 			return;
 
 		parent.bInWork	= true;
 
 		if (parent.TimeoutID != null)
 			parent.clearTimeout(parent.TimeoutID);
 
 		parent.bLayter = true;
 
 		if ('<#cmd#>' == '') {
 			var newsrv = parent.getServerName(document.forms[0].frameid.value);
 			if (newsrv == '') newsrv = '<#msrvr#>';
 			if ( ((document.cookie != '') && (parent.GetCookie('cursrv') != newsrv)) || ((document.cookie == '') && (parent.main_server != newsrv)) )
 			{
 				if (newsrv == '<#msrvr#>') {
 					document.cookie = "cursrv=" + newsrv + "; path=\/";
 					parent.expdate = -1;
 				} else {
 					var d = new Date();
 					d.setTime(d.getTime() + <#exptime#>*60000);
 					var Ex	= d.toGMTString();
 					document.cookie = "cursrv=" + newsrv + "; expires=" + Ex + "; path=\/";
 					parent.expdate = d.getTime();
 				}
 
 				parent.main_server = newsrv;
 				parent.second_servers = parent.GetSecondSrvrs(newsrv);
 				parent.fsec_srvrs = parent.second_servers.split(";");
 			}
 		}
 
 		var f	= document.forms[0];
 		if ('<#result#>' != '0' || !window.confirm('Your message has a spelling error.\nDo you want to open the SpellChecker?'))
 		{
 			if (parent.cTL) {
 				if ('<#result#>' == '1')
 				parent.lastresult = true;
 				parent.cTL.form.submit();
 			}
 			return;
 		}
 
 		parent.<#frameref#>['<#sp_f_name#>'].document.forms[0].cck_text.value = parent.cTL.value;
 
 		if (parent.IsProxyVer) {
 			var msrv = parent.getServerName(document.forms[0].frameid.value);
 			if (parent.<#frameref#>['<#sp_f_name#>'].ssrv_host != msrv)
 				parent.<#frameref#>['<#sp_f_name#>'].ssrv_host = msrv;
 		} else {
 			if (parent.<#frameref#>['<#sp_f_name#>'].service_host != document.forms[0].frameid.value) {
 				parent.<#frameref#>['<#sp_f_name#>'].is_window_opened = false;
 				parent.<#frameref#>['<#sp_f_name#>'].service_host = document.forms[0].frameid.value;
 			}
 		}
 
 		parent.<#frameref#>['<#sp_f_name#>'].doSpell(parent.sLang, parent.<#frameref#>['<#sp_f_name#>'].document.forms[0].cck_text, "<#self_name#>", true, parent.sLang);
 	}
 	// -->
 	</script>
 	</head>
 	<body onload="javascript:doload();" bgcolor=#FFFFFF>
 		<form method=post action="<#self_name#>">
 			<input type=hidden name=cmd value="acheck">
 			<input type=hidden name=trg_url value="<#self_name#>">
 			<input type=hidden name=slang value="<#lang#>">
 			<input type=hidden name=text value="">
 			<input type=hidden name=frameid value="<#frameid#>">
 			<input type=hidden name=ssrv_host value="">
 			<input type=hidden name=ssrv_port value="">
 			<input type=hidden name=ssrv_path value="">
 			<input type=hidden name=customerid value="">
 		</form>
 	</body>
 </html>
 <!-- <#achk_ver#> -->	
